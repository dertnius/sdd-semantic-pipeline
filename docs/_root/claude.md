# Architecture & guardrails (CLAUDE.md)

> Rendered from the canonical `CLAUDE.md` at the repository root — the architecture
> map, module boundaries, and guardrails. Edit that file (not this stub); this page is
> an in-site mirror of it.

{%
   include-markdown "../../CLAUDE.md"
   heading-offset=1
%}
